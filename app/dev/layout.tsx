export const metadata = {
  title: "Dev Panel | Biokiste",
  description: "Simulation controls for development and testing",
};

export default function DevLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
